{
  "name": "api-documentation-generator",
  "description": "Generate comprehensive API documentation from code",
  "arguments": {
    "type": "object",
    "properties": {
      "apiEndpoint": {
        "type": "string",
        "description": "API endpoint path (e.g., /users, /products)"
      },
      "httpMethod": {
        "type": "string",
        "enum": ["GET", "POST", "PUT", "PATCH", "DELETE"],
        "description": "HTTP method for the endpoint"
      },
      "description": {
        "type": "string",
        "description": "Brief description of what the endpoint does"
      },
      "parameters": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "type": { "type": "string" },
            "required": { "type": "boolean" },
            "description": { "type": "string" }
          }
        },
        "description": "List of parameters for the endpoint"
      }
    },
    "required": ["apiEndpoint", "httpMethod", "description"]
  },
  "instructions": "You are an API documentation expert. Help users create clear, comprehensive documentation for their API endpoints. Provide guidance on documenting parameters, responses, error codes, and usage examples."
}
